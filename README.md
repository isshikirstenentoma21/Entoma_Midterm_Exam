# Entoma Midterm Exam (Flutter UI)

This repository contains a Flutter UI implementation that matches the provided mockup images.

## Run
1. Install Flutter
2. From the project root:
   ```bash
   flutter pub get
   flutter run
   ```

